# ===== ROS 2 / colcon build artifacts =====
build/
install/
log/

# ===== Python: byte-compiled / optimized / cache =====
__pycache__/
*.py[cod]
*$py.class
*.so

# ===== Python: packaging / distribution =====
.Python
develop-eggs/
dist/
downloads/
eggs/
.eggs/
sdist/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# ===== Python: virtual environments =====
.venv/
venv/
env/
ENV/
env.bak/
venv.bak/

# ===== Python: test / coverage / type-check caches =====
.pytest_cache/
.tox/
.nox/
.coverage
.coverage.*
htmlcov/
coverage.xml
*.cover
.cache
.mypy_cache/
.dmypy.json
.pyre/
.pytype/
.ruff_cache/

# ===== Jupyter =====
.ipynb_checkpoints/

# ===== Editor / OS =====
*.swp
*.swo
*~
.vscode/
.idea/
*.code-workspace
.DS_Store
Thumbs.db
*.pt

# Webots per-user project metadata.
webots_worlds/*.wbproj

# ===== Personal notes (not part of the package) =====
指示内容.md

# ===== Codex continuous improvement runtime state =====
.codex/auto_improve_state.json
.codex/auto_improve.stop
.codex/auto_improve_runs/

# ===== 保存地図の PGM / 確認用オーバーレイ PNG =====
# PGM は frontier 探索/SLAM で生成する占有格子地図（generate_waypoints.py の入力）。
# 後段の Nav2 / waypoint / 認識評価が YAML だけ残る状態で壊れないよう、PGM は追跡対象にする。
# PNG は generate_waypoints.py がウェイポイントを地図に重ねた確認用画像（再生成可能）。
# 確認用 PNG は各自の環境で再生成する前提なので追跡しない。
maps/*.png
maps/*_crops/
maps/*_object_assoc/
maps/indoor_localization_cycle*.csv
maps/indoor_localization_cycle*.json
maps/indoor_localization_cycle*.md
maps/indoor_recognition_cycle*.csv
maps/indoor_recognition_cycle*.json
maps/indoor_recognition_cycle*.md
maps/indoor_recognition_cycle*.yaml
maps/*_vs_world.json

# ===== 色付き点群 PLY (大容量バイナリ、リポジトリに含めない) =====
# colorized_pointcloud_mapper が omni camera + LiDAR fusion で作る色付き点群地図。
# /slam/save_colorized_map サービスで maps/colorized/<world>_colored.ply に保存する。
# 各自の環境で再生成する前提（数MB〜。プロジェクト内に置くが追跡しない）。
maps/colorized/

# ===== MID-360 scan pattern CSV (大容量 ~40MB、リポジトリに含めない) =====
# Gazebo MID-360 プラグイン(liblivox_mid360_sensor)が読む scan pattern。
# 取得元・配置手順は docs/mid360_lidar_research.md 参照。
config/mid360_scan_patterns/*.csv
